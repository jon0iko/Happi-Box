return (
    <div className="container mx-auto p-4">
      <h1>Product Page</h1>
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {items.map((item, itemIndex) => {
          // Find the index of the selected option in the options array
          const selectedOptionIndex = selectedOptions[itemIndex];
  
          // Set the price based on the selected option
          const selectedPrice = item.price[selectedOptionIndex];
  
          // Format the selected price string
          const formattedPrice = `৳${selectedPrice.toFixed(2)}`;
  
          return (
            <div key={item.id} className="bg-white p-4 rounded-lg shadow-md">
              {/* ... (your existing code for item display) */}
              <p className="text-green-600 font-semibold mt-2">{formattedPrice}</p>
              {/* ... (remaining code) */}
            </div>
          );
        })}
      </div>
    </div>
  );
  